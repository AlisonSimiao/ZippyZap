# Environment Variables for ZippyZap

# Production domain (used for canonical URLs, Open Graph, etc.)
NEXT_PUBLIC_APP_URL=https://zippyzap.online

# API endpoint (with api. subdomain prefix)
NEXT_PUBLIC_API_URL=https://api.zippyzap.online
