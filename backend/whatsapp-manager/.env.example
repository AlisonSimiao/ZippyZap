# Server
PORT=8090

# Webhook — receives all session events (POST, JSON payload)
WEBHOOK_URL=http://localhost:8080/webhooks/wuzapi

# Data directory — stores SQLite databases and session files
DATA_DIR=./data

# Log level: debug, info, warn, error
LOG_LEVEL=info
